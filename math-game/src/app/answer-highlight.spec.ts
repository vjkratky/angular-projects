import { AnswerHighlight } from './answer-highlight';

describe('AnswerHighlight', () => {
  it('should create an instance', () => {
    const directive = new AnswerHighlight();
    expect(directive).toBeTruthy();
  });
});
