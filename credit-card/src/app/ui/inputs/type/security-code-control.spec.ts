import { SecurityCodeControl } from './security-code-control';

describe('SecurityCodeControl', () => {
  it('should create an instance', () => {
    expect(new SecurityCodeControl()).toBeTruthy();
  });
});
